const cart=[];const count=document.getElementById('cartCount');const toast=document.getElementById('toast');function showToast(text){toast.textContent=text;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1800)}document.querySelectorAll('.add-button').forEach(button=>button.addEventListener('click',()=>{cart.push({name:button.dataset.name,price:Number(button.dataset.price)});count.textContent=cart.length;showToast(`${button.dataset.name} added to bag`)}));document.getElementById('cartButton').addEventListener('click',()=>{if(!cart.length){showToast('Your bag is empty');return}const total=cart.reduce((sum,item)=>sum+item.price,0);showToast(`${cart.length} item${cart.length>1?'s':''} • ₹${total.toLocaleString('en-IN')}`)});document.getElementById('subscribeForm').addEventListener('submit',event=>{event.preventDefault();document.getElementById('formMessage').textContent='Thanks — you’re on the list.';event.target.reset()});